# PMIRI GC-C1 Isolation Adapters 0.1.0

The adapter layer accepts only explicit observed evidence for network,
credentials, filesystem, connectors, determinism, resource limits, teardown
and privacy. Missing, unverified or failed evidence is rejected. It does not
probe the current host, claim isolation, launch the runner or perform replay.
